<form method="post" action="calendar.html.twig">
    <input type="hidden" value="{{ item.id }}" name="id">
    <input type="text" value="{{ item.title }}" name="title">
    <input type="submit" value="Valider">
</form>
